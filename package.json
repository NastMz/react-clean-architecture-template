{
  "name": "foundry-cli",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -C packages/template build",
    "format": "pnpm -C packages/template format",
    "lint": "pnpm -C packages/template lint",
    "test": "pnpm -C packages/template test",
    "typecheck": "pnpm -C packages/template typecheck"
  },
  "pnpm": {
    "overrides": {
      "vite": "^6.4.1"
    }
  }
}
